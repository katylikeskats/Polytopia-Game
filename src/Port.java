/**
 * [Port.java]
 * The port object class, which is a type of water-based terrain
 * @author Katelyn Wang and Brian Li
 * June 14 2018
 */
public class Port extends Water {
    /**
     * Boat Constructor
     * @param row The port's row coordinate
     * @param col The ports' column coordinate
     */
    Port(int row, int col) {
        super(row, col);
    }
}
